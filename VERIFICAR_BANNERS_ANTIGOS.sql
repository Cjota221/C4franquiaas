-- Verificar banners na estrutura ANTIGA (que podem ter sumido)

-- 1. Ver se existe a tabela antiga
SELECT * FROM banner_personalizacoes LIMIT 5;

-- OU se for outro nome:
-- SELECT * FROM banner_submissions_old LIMIT 5;
-- SELECT * FROM banners LIMIT 5;
