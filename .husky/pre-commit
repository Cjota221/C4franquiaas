
npm run lint:fix
# Run format only if prettier is installed to avoid failing commits before deps are installed
if command -v prettier >/dev/null 2>&1; then
	npm run format
else
	echo "prettier not found, skipping format (run 'npm install' to enable)"
fi
